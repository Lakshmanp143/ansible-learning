- name: loop
  hosts: local
  connection: local
  tasks:
  - name: print names
    ansible.builtin.debug:
      msg: "Hello {{ items }}"
    loop:
    - laxman
    - ram
    - subbu
    - vinay
    - anu